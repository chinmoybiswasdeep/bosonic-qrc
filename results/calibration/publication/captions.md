# Figure captions

- Capacity panels show raw held-out C=max(0,R²) and FDR-significant capacity.
- Rank panels show centered feature singular spectra; numerical thresholds combine absolute and relative tolerances.
- CV quantities are temporal IPC. DV quantities are delay-zero static nonlinear capacity and are not temporal memory.
